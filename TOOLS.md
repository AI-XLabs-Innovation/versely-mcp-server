# Versely MCP Tool Catalog

> Auto-generated by `npm run docs` on 2026-05-07. Do not edit by hand — regenerate after changing any tool definition.

**51 tools** across **8 categories**. All async generation tools default to `mode: "wait"` (poll until done) — pass `mode: "submit"` to fire-and-forget and poll later via `versely_get_task_status` / `versely_wait_for_task`.

## Contents

- [User & Account](#user-account)
- [Generation (image / video / audio / music)](#generation-image-video-audio-music)
- [Slideshow](#slideshow)
- [Movie (multi-scene)](#movie-multi-scene)
- [UGC (overlays + captions)](#ugc-overlays-captions)
- [Social media](#social-media)
- [Status / async polling](#status-async-polling)
- [Utilities](#utilities)

## User & Account

_6 tools_

### `versely_get_me`

Get the authenticated user's profile, plan, and credit balance. Returns the full /user/me response.

_No input parameters._

### `versely_get_credits`

Get the credit balance for the current user (or a specified user_id).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `user_id` | string | — | Override the user_id; defaults to the authenticated user. |

### `versely_list_api_key_scopes`

List all API-key scopes available on the Versely platform (catalog).

_No input parameters._

### `versely_list_purchases`

Get the credit purchase history for the authenticated user.

_No input parameters._

### `versely_list_user_media`

List the user's generated media of a given type (images, videos, audios, music, slideshows, ugc), paginated.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | enum("images" \| "videos" \| "audios" \| "music" \| "slideshows" \| "ugc") | **yes** | Media category to list. |
| `user_id` | string | — | Override the user_id; defaults to the authenticated user. |
| `page` | integer | — |  |
| `limit` | integer | — |  |

### `versely_delete_generation`

Delete a specific generation (image, video, audio, or music) by ID.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | enum("image" \| "video" \| "audio" \| "music") | **yes** | Generation type. |
| `generation_id` | string | **yes** | ID of the generation to delete. |

## Generation (image / video / audio / music)

_10 tools_

### `versely_list_models`

List supported AI models. Optionally filter by media type (image / video / audio / lipsync) or provider.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | enum("image" \| "video" \| "audio" \| "lipsync" \| "all") | — | Filter by media type. Defaults to all. |
| `provider` | string | — | Filter by provider slug (e.g. 'fal', 'kie', 'runpod', 'replicate', 'suno'). |

### `versely_generate_image`

Generate one or more images (text-to-image, image-to-image, editing) using a chosen model. Default polls until done.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | Model slug (e.g. 'flux-2', 'imagen-4', 'nano-banana-2', 'midjourney-v7', 'gpt-image-2', 'recraft-4'). |
| `prompt` | string | **yes** | Text prompt describing the desired image. |
| `negative_prompt` | string | — |  |
| `n` | integer | — | Number of images to generate. |
| `aspect_ratio` | string | — | e.g. '1:1', '16:9', '9:16', '4:3'. |
| `size` | string | — | Explicit dimensions, e.g. '1024x1024'. |
| `image_urls` | string (url)[] | — | Reference images for image-to-image / editing modes. |
| `seed` | integer | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_video`

Generate a video (text-to-video, image-to-video, frame-to-frame) using a chosen model. Default polls until done.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | Model slug (e.g. 'sora-2', 'veo-3.1', 'kling-2.5', 'hailuo-2.3', 'seedance-2.0', 'ltx-2.3'). |
| `prompt` | string | **yes** | Text prompt for the video. |
| `image_url` | string (url) | — | Starting frame for image-to-video. |
| `end_image_url` | string (url) | — | End frame for frame-to-frame. |
| `duration_seconds` | number | — |  |
| `aspect_ratio` | string | — |  |
| `resolution` | string | — |  |
| `seed` | integer | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_audio`

Generate speech / audio via TTS. Default polls until done.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | TTS model slug (e.g. 'eleven-labs-multilingual-v2', 'cartesia-sonic'). |
| `text` | string | **yes** |  |
| `voice` | string | — |  |
| `voice_id` | string | — |  |
| `language` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_music`

Generate music with Suno (V3.5–V5.5). Returns a Suno taskId; default polls via the unified status endpoint.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `prompt` | string | **yes** | Style / mood / theme prompt. |
| `lyrics` | string | — | Custom lyrics (omit for instrumental or auto-lyrics). |
| `title` | string | — |  |
| `tags` | string | — | Comma-separated style tags (e.g. 'pop, upbeat, electronic'). |
| `instrumental` | boolean | — |  |
| `model_version` | string | — | e.g. 'V4', 'V5'. |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_extend_music`

Extend an existing Suno track from a given timestamp.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `task_id` | string | **yes** | Suno task_id of the source track. |
| `audio_id` | string | **yes** | Specific audio variant ID within the task. |
| `continue_at_seconds` | number | **yes** | Position in seconds to continue from. |
| `prompt` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_lipsync`

Generate a lipsync video from a still image and an audio clip.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `image_url` | string (url) | **yes** |  |
| `audio_url` | string (url) | **yes** |  |
| `model` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_remove_background`

Remove the background from an image.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `image_url` | string (url) | **yes** |  |
| `model` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_upscale_image`

Upscale an image to higher resolution.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `image_url` | string (url) | **yes** |  |
| `model` | string | — |  |
| `scale_factor` | number | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_upscale_video`

Upscale a video to higher resolution.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `model` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

## Slideshow

_8 tools_

### `versely_create_slideshow`

Create a slideshow by generating multiple AI images from a prompt (no automation, no overlays).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `prompt` | string | **yes** |  |
| `model` | string | — |  |
| `n` | integer | — |  |
| `aspect_ratio` | string | — |  |

### `versely_create_automated_slideshow`

Full automation: AI plans the slideshow, generates the images, and burns text overlays in one request.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `topic` | string | **yes** | Topic / theme for the slideshow. |
| `n_slides` | integer | — |  |
| `style` | string | — |  |
| `aspect_ratio` | string | — |  |
| `model` | string | — |  |

### `versely_get_slideshow`

Get a slideshow by ID, including its images.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |

### `versely_list_slideshows`

List the authenticated user's slideshows (paginated).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `page` | integer | — |  |
| `limit` | integer | — |  |

### `versely_delete_slideshow`

Delete a slideshow and all its images.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |

### `versely_add_slideshow_images`

Generate and append more AI images to an existing slideshow.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |
| `prompt` | string | **yes** |  |
| `n` | integer | — |  |
| `model` | string | — |  |

### `versely_add_text_overlay`

Burn text overlays onto a slideshow's images. Pass an array of overlays (one per slide or shared).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |
| `overlays` | object[] | **yes** |  |

### `versely_slideshow_to_video`

Compile a slideshow's images (and optional audio) into an MP4 video with transitions (server-side FFmpeg).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |
| `duration_per_slide_seconds` | number | — |  |
| `transition` | string | — | e.g. 'fade', 'slide', 'crossfade'. |
| `audio_url` | string (url) | — |  |
| `aspect_ratio` | string | — |  |

## Movie (multi-scene)

_7 tools_

### `versely_create_movie`

Create a multi-scene movie project. Scenes are stored but NOT generated yet — call versely_generate_movie_scenes to start.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `title` | string | **yes** |  |
| `description` | string | — |  |
| `aspect_ratio` | string | — |  |
| `default_model` | string | — |  |
| `scenes` | object[] | **yes** |  |

### `versely_list_movies`

List the authenticated user's movies (paginated).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `page` | integer | — |  |
| `limit` | integer | — |  |

### `versely_get_movie`

Get a movie with all its scenes and metadata.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_delete_movie`

Delete a movie and all its scenes.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_get_movie_status`

Get real-time generation status for a movie and each of its scenes (preferred over per-scene status polling).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_generate_movie_scenes`

Kick off generation for all pending scenes in a movie. Returns immediately; poll versely_get_movie_status to track progress.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_combine_movie`

Once all scenes are completed, combine them into the final movie video (server-side FFmpeg).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |
| `transition` | string | — |  |
| `audio_url` | string (url) | — |  |

## UGC (overlays + captions)

_5 tools_

### `versely_add_video_overlay`

Overlay a foreground video (e.g. talking head) on top of a base video, positioned in a corner.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `base_video_url` | string (url) | **yes** |  |
| `overlay_video_url` | string (url) | **yes** |  |
| `position` | enum("top-left" \| "top-right" \| "bottom-left" \| "bottom-right") | — |  |
| `scale` | number | — | Overlay size as a fraction of base video width (0-1). |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_add_captions`

Add a single static caption to a video at top / middle / bottom.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `caption_text` | string | **yes** |  |
| `position` | enum("top" \| "middle" \| "bottom") | — |  |
| `font` | string | — |  |
| `font_size` | integer | — |  |
| `color` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_add_timestamped_captions`

Auto-transcribe a video's audio and burn timestamped captions onto it (Reels-style).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `style` | string | — | Caption style preset. |
| `position` | enum("top" \| "middle" \| "bottom") | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_compose_with_overlay`

Stitch a sequence of images/videos into a base composition and optionally overlay another video on top.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `media_urls` | string (url)[] | **yes** | Images or video clips to stitch. |
| `overlay_video_url` | string (url) | — |  |
| `overlay_position` | enum("top-left" \| "top-right" \| "bottom-left" \| "bottom-right") | — |  |
| `audio_url` | string (url) | — |  |
| `duration_per_item_seconds` | number | — |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_get_ugc`

Get a UGC video by ID.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ugc_id` | string | **yes** |  |

## Social media

_8 tools_

### `versely_get_social_auth_url`

Get an OAuth URL the user can open in a browser to connect a social account to their Versely account.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `platform` | enum("instagram" \| "tiktok" \| "youtube" \| "twitter" \| "facebook" \| "linkedin" \| "pinterest" \| "bluesky" \| "threads") | **yes** | Which platform to connect. |
| `redirect_url` | string (url) | — | Where to send the user after OAuth completes. |

### `versely_list_social_accounts`

List all social media accounts connected to the authenticated user.

_No input parameters._

### `versely_refresh_social_accounts`

Refresh OAuth tokens and sync the latest follower counts / metadata for connected accounts.

_No input parameters._

### `versely_disconnect_social_account`

Disconnect a connected social account by accountId.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `account_id` | string | **yes** |  |

### `versely_preview_post`

Preview how a post will appear on each target platform without publishing or charging credits.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `caption` | string | — |  |
| `media_urls` | string (url)[] | — |  |
| `targets` | object[] | **yes** |  |

### `versely_publish_post`

Publish (or schedule) a post to one or more connected social accounts via Post for Me.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `caption` | string | — |  |
| `media_urls` | string (url)[] | — |  |
| `targets` | object[] | **yes** |  |
| `scheduled_at` | string | — | ISO-8601 timestamp to schedule the post; omit to publish immediately. |

### `versely_list_posts`

List the authenticated user's published posts.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `page` | integer | — |  |
| `limit` | integer | — |  |
| `platform` | enum("instagram" \| "tiktok" \| "youtube" \| "twitter" \| "facebook" \| "linkedin" \| "pinterest" \| "bluesky" \| "threads") | — |  |

### `versely_get_post`

Get a published post's details and performance metrics.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `post_id` | string | **yes** |  |

## Status / async polling

_2 tools_

### `versely_get_task_status`

Get the current status of an async generation task by request_id (single, non-blocking lookup).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `request_id` | string | **yes** | The request_id / task_id returned by a generation tool. |

### `versely_wait_for_task`

Block and poll a request_id until it reaches a terminal state (completed/failed) or the timeout elapses.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `request_id` | string | **yes** | The request_id / task_id to wait on. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms. |
| `poll_interval_ms` | integer | — | Override default poll interval in ms. |

## Utilities

_5 tools_

### `versely_extract_frames`

Extract frames from a video at given timestamps or evenly spaced.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `timestamps_seconds` | number[] | — |  |
| `count` | integer | — | Evenly extract this many frames if timestamps are not given. |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_merge_videos`

Merge multiple videos into a single video (server-side FFmpeg).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_urls` | string (url)[] | **yes** |  |
| `transition` | string | — | Optional transition (e.g. 'fade', 'crossfade'). |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_prompt`

Use Gemini to expand a brief idea into a richer image / video / music generation prompt.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `idea` | string | **yes** | Short concept to expand into a full prompt. |
| `target` | enum("image" \| "video" \| "music" \| "any") | — | Target media type for the prompt style. |
| `style` | string | — |  |

### `versely_colorize_photo`

Colorize a grayscale or faded photo.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `image_url` | string (url) | **yes** |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_audio_isolation`

Isolate vocals (or remove background noise) from an audio clip.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `audio_url` | string (url) | **yes** |  |
| `mode` | enum("wait" \| "submit") | — | 'wait' (default) blocks and polls until completion; 'submit' returns the request_id immediately so the caller can poll later via versely_get_task_status. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

