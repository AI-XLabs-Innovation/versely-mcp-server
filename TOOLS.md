# Versely MCP Tool Catalog

> Auto-generated by `npm run docs` on 2026-09-24. Do not edit by hand — regenerate after changing any tool definition.

**119 tools** across **15 categories** (the full profile — claude.ai, Claude Code, Cursor, API keys). 105 of them are also in the ChatGPT plugin's `openai` profile, where some inputs are hidden and model lists are limited to the models the plugin can serve (see README → Profiles).

Async generation tools default to `mode: "submit"`: they return a `request_id` right away while the job runs in the background. Hosts that render the inline media card update it by themselves; otherwise check the job with `versely_get_task_status`. `mode: "wait"` blocks for at most ~70 seconds and is not offered to ChatGPT.

## Contents

- [User & Account](#user-account)
- [Billing & subscription](#billing-subscription)
- [Generation (image / video / audio / music)](#generation-image-video-audio-music)
- [Music + sound effects](#music-sound-effects)
- [Slideshow](#slideshow)
- [Movie (multi-scene)](#movie-multi-scene)
- [UGC (overlays + captions)](#ugc-overlays-captions)
- [Social media](#social-media)
- [Status / async polling](#status-async-polling)
- [Utilities](#utilities)
- [Workflows](#workflows)
- [Video-workflow runs](#video-workflow-runs)
- [Voices](#voices)
- [Dubbing](#dubbing)
- [Automations](#automations)

## User & Account

_6 tools_

### `versely_get_me`

**Get account profile** · read (read-only) · also in the ChatGPT plugin

Get the authenticated user's profile (id, email, name) and credit balance. Returns the full /user/me response.

_No input parameters._

### `versely_get_credits`

**Get credit balance** · read (read-only) · also in the ChatGPT plugin

Get the credit balance for the current user (or a specified user_id).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `user_id` | string | — | Override the user_id; defaults to the authenticated user. |

### `versely_list_api_key_scopes`

**List API key scopes** · read (read-only)

List all API-key scopes available on the Versely platform (catalog).

_No input parameters._

### `versely_list_purchases`

**List purchases** · read (read-only) · also in the ChatGPT plugin

The user's transaction history: every subscription payment and credit-pack purchase, newest first, with the credits each added, its status and date, plus the total credits purchased.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `category` | enum("subscription" \| "credit_pack") | — | Only subscription payments, or only credit packs. Omit for both. |
| `limit` | integer | — | How many (default 50). |
| `offset` | integer | — | How many to skip, for paging. |

### `versely_list_user_media`

**List my media** · read (read-only) · also in the ChatGPT plugin

List the user's generated media of a given type (images, videos, audios, music, slideshows, ugc), paginated.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | enum("images" \| "videos" \| "audios" \| "music" \| "slideshows" \| "ugc") | **yes** | Media category to list. |
| `user_id` | string | — | Override the user_id; defaults to the authenticated user. |
| `limit` | integer | — | Page size (default 10, server caps at 50). |
| `offset` | integer | — | How many to skip (default 0). Use for paging, not `page`. |

### `versely_delete_generation`

**Delete a generation** · delete (destructive) · also in the ChatGPT plugin

Delete a generation by ID. The plain types delete the media row; the '-row' variants delete the PARENT generation record — use those to clear a failed/pending generation that has no output URL (e.g. a failure tile).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | enum("image" \| "video" \| "audio" \| "music" \| "slideshow" \| "ugc" \| "image-row" \| "video-row") | **yes** | Generation type. Use 'image-row'/'video-row' to remove a failed generation. |
| `generation_id` | string | **yes** | ID of the generation to delete. |

## Billing & subscription

_12 tools_

### `versely_get_subscription`

**Get subscription** · read (read-only) · also in the ChatGPT plugin

The user's Versely subscription: plan, status (active, paused, cancelled...), whether it is on a free trial, when it renews or ends, whether it is set to cancel, and any plan switch scheduled.

_No input parameters._

### `versely_list_credit_history`

**List credit history** · read (read-only) · also in the ChatGPT plugin

The user's credit history, newest first: what each generation spent (with its model), refunds for failed jobs, and credits added by purchases, with the balance after each.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `limit` | integer | — | How many entries (default 50). |
| `offset` | integer | — | How many to skip, for paging. |

### `versely_list_plans`

**List plans and credit packs** · read (read-only)

Versely's subscription plans and credit packs: key, name, price (USD) and credits. Credit packs are add-ons that top up a plan: they can only be bought while the account has an active subscription. Also says whether free-trial spots are open (the trial comes with a subscription, for accounts that never subscribed).

_No input parameters._

### `versely_create_checkout_link`

**Create checkout link** · create (open-world)

Create a Dodo Payments checkout link for a subscription plan or a credit pack (`plan_key` from versely_list_plans), and give it to the user to open and pay. Nothing is charged until they pay there. A credit pack needs an active subscription; an account that already has a plan switches with versely_change_plan instead. `trial: true` starts a subscription with the free trial, for accounts that never subscribed while spots are open.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `plan_key` | string | **yes** | The plan or credit pack `key` from versely_list_plans. |
| `trial` | boolean | — | Start the subscription with the free trial (subscription plans only). |

### `versely_get_billing_portal_link`

**Get billing portal link** · read (read-only, open-world)

A link to the user's Dodo Payments billing portal, where they update their card and download invoices. Only for accounts that have bought something on the web.

_No input parameters._

### `versely_cancel_subscription`

**Cancel subscription** · delete (destructive, open-world)

Cancel the user's web subscription at the end of the current period: they keep the plan and its credits until then (`access_until`), and it does not renew. Undo with versely_resume_subscription before it ends. Confirm with the user first. Versely asks three short questions first: ask the user (don't guess) why, what would have kept them, and whether they might come back, and pass their answers. App-store subscriptions are cancelled in the store.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `reason` | enum("too_expensive" \| "unused" \| "missing_features" \| "low_quality" \| "switched_service" \| "too_complex" \| "customer_service" \| "other") | **yes** | Why: too_expensive, unused, missing_features, low_quality, switched_service, too_complex, customer_service, other. |
| `improvement` | enum("lower_price" \| "more_credits" \| "specific_model" \| "better_quality" \| "faster_generations" \| "easier_to_use" \| "nothing") | **yes** | What would have kept them: lower_price, more_credits, specific_model, better_quality, faster_generations, easier_to_use, nothing. |
| `return_intent` | enum("soon" \| "maybe_later" \| "unlikely" \| "unsure") | **yes** | Would they come back: soon, maybe_later, unlikely, unsure. |

### `versely_pause_subscription`

**Pause subscription** · edit (destructive, open-world)

Pause the user's web subscription now: billing stops, and so do the plan's benefits, until it is resumed with versely_resume_subscription. Confirm with the user first. Versely asks three short questions first: ask the user (don't guess) why, what would have kept them, and whether they might come back, and pass their answers.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `reason` | enum("too_expensive" \| "unused" \| "missing_features" \| "low_quality" \| "switched_service" \| "too_complex" \| "customer_service" \| "other") | **yes** | Why: too_expensive, unused, missing_features, low_quality, switched_service, too_complex, customer_service, other. |
| `improvement` | enum("lower_price" \| "more_credits" \| "specific_model" \| "better_quality" \| "faster_generations" \| "easier_to_use" \| "nothing") | **yes** | What would have kept them: lower_price, more_credits, specific_model, better_quality, faster_generations, easier_to_use, nothing. |
| `return_intent` | enum("soon" \| "maybe_later" \| "unlikely" \| "unsure") | **yes** | Would they come back: soon, maybe_later, unlikely, unsure. |

### `versely_resume_subscription`

**Resume subscription** · edit (open-world)

Resume the user's paused web subscription, or undo a scheduled cancellation so it renews again.

_No input parameters._

### `versely_skip_trial`

**End free trial now** · edit (open-world)

End the user's free trial now and start the paid plan: their card is charged now (instead of when the trial ends) and the plan's full credits are added. Web trials only. Confirm with the user first.

_No input parameters._

### `versely_preview_plan_change`

**Preview plan change** · read (read-only)

What switching the user's subscription to another plan would do, without changing anything: when it takes effect (now or at the next renewal), what is charged today, the credits they get now, and the new renewal date. Show this to the user before versely_change_plan.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `plan_key` | string | **yes** | The target plan `key` from versely_list_plans. |

### `versely_change_plan`

**Change plan** · edit (destructive, open-world)

Switch the user's web subscription to another plan (as versely_preview_plan_change describes: upgrades apply now and may charge today, downgrades at the next renewal). Confirm with the user after showing the preview.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `plan_key` | string | **yes** | The target plan `key` from versely_list_plans. |

### `versely_cancel_plan_change`

**Cancel plan change** · edit (open-world)

Cancel a plan switch that is scheduled for the next renewal; the current plan simply continues.

_No input parameters._

## Generation (image / video / audio / music)

_10 tools_

### `versely_find_models`

**Find models** · read (read-only) · also in the ChatGPT plugin

Discover AI models for image / video / audio / lipsync generation. ALWAYS call this before versely_generate_image / _video / _audio / _lipsync — guessing leads to 'Model not supported'.

Pass the returned `name` (or `slug`) to the generate tools. `display_name`, when present, is only a human label — the dispatcher does not accept it.

`credits` is indicative RELATIVE cost, not the amount you'll be charged — see credits_note in the response. Models with `min_credits`/`max_credits` are priced per resolution/duration.

Feature models (upscale, background removal, colorization) and unsupported-surface models (storyboard, inpainting) are excluded by default — they have dedicated tools (versely_upscale_image / versely_upscale_video / versely_remove_background / versely_colorize_photo) and would fail here. Set include_feature_models to see them.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | enum("image" \| "video" \| "audio" \| "lipsync") | — | Filter by media type. If omitted, searches across all four types. |
| `include_feature_models` | boolean | — | Include upscale / background-removal / colorization / storyboard / inpaint models, which are hidden by default. They are not callable via the generate tools. |
| `q` | string | — | Case-insensitive substring match on model slug + name (e.g. 'flux ultra', 'kling', 'sora'). |
| `provider` | string | — | Filter by provider name (e.g. 'Flux', 'OpenAI', 'Google', 'ByteDance'). Case-insensitive. |
| `category` | string | — | Filter by category. Valid values per type: image → 'text-to-image', 'image-to-image', 'edit-image'; video → 'text-to-video', 'image-to-video', 'edit-video', 'reference-to-video', 'extend-video', 'motion-control', 'audio-to-video'; audio → 'text-to-audio', 'voice-clone', 'audio-to-audio', 'audio-to-text'; lipsync → 'text-to-lipsync', 'image-to-lipsync', 'audio-to-lipsync', 'video-to-lipsync'. |
| `is_featured` | boolean | — | Only return featured models. |
| `is_premium` | boolean | — | If true, only premium models; if false, only non-premium. |
| `limit` | integer | — | Max results returned. Default 30. |

### `versely_get_model_inputs`

**Get model inputs** · read (read-only) · also in the ChatGPT plugin

Get the inputs a model accepts — names, types, allowed values, defaults, and (for voiceover models) its voices — so you can fill them in instead of asking the user. Pass a model `name` from versely_find_models. Send the returned params as top-level arguments to the matching generate tool (versely_generate_image / versely_generate_video / versely_generate_audio), next to `model` and the prompt. When a model has no detailed input list, the reply says so and gives what the catalog knows.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | A model `name` from versely_find_models. |

### `versely_list_models`

**List all models** · read (read-only)

List supported AI models with full metadata (rankings, descriptions, etc.). Response is large — prefer versely_find_models for discovery; use this only when full metadata is genuinely needed. Optionally filter by media type or provider.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | enum("image" \| "video" \| "audio" \| "lipsync" \| "all") | — | Filter by media type. Defaults to all. |
| `provider` | string | — | Filter by the catalog's `provider` value, matched exactly server-side. This is the model's brand/vendor as stored on the row (what versely_find_models reports as `provider`) — NOT the routing backend ('fal' / 'kie' / 'runpod'), which is chosen server-side and is not filterable. If unsure, omit this and filter the results yourself; a wrong value silently returns zero models. |

### `versely_generate_image`

**Generate image** · create · also in the ChatGPT plugin

Generate one or more images (text-to-image, image-to-image, editing) using a chosen model. Returns a request_id immediately (submit mode) while the job runs in the background. Hosts that show the inline media card update it by themselves; otherwise check the job with versely_get_task_status.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | Image model — pass either the slug (e.g. 'flux-pro-ultra') or the canonical name (e.g. 'Flux Pro Ultra'). The MCP resolves either form to the dispatcher's expected name. Call versely_find_models with type='image' first to discover valid identifiers. |
| `prompt` | string | **yes** | Text prompt describing the desired image. |
| `negative_prompt` | string | — |  |
| `num_images` | integer | — | Number of images to generate (default 1). Each image is charged separately. |
| `n` | integer | — | Deprecated alias for num_images — prefer num_images. |
| `aspect_ratio` | string | — | e.g. '1:1', '16:9', '9:16', '4:3'. |
| `resolution` | string | — | Resolution tier for resolution-priced models (e.g. '1K', '2K', '4K' for GPT Image 2 / Nano Banana Pro). Higher tiers cost more credits. Note: '1:1' cannot use 4K, and aspect_ratio 'auto' is limited to 1K. |
| `image_urls` | string (url)[] | — | Reference images for image-to-image / editing modes. |
| `seed` | integer | — |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_video`

**Generate video** · create · also in the ChatGPT plugin

Generate a video (text-to-video, image-to-video, frame-to-frame) using a chosen model. Returns a request_id immediately (submit mode) while the job runs in the background. Hosts that show the inline media card update it by themselves; otherwise check the job with versely_get_task_status.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | Video model — pass either the slug or the canonical name. Call versely_find_models with type='video' first to discover valid identifiers. |
| `prompt` | string | **yes** | Text prompt for the video. |
| `image_url` | string (url) | — | Starting frame for image-to-video. |
| `image_urls` | string (url)[] | — | Starting frame(s) for image-to-video. Equivalent to image_url; use either. Required by models whose name contains 'Image to Video'. |
| `end_image_url` | string (url) | — | End frame for frame-to-frame (first-last-frame) models. |
| `duration_seconds` | number | — | Clip length in seconds. Drives BOTH generation and credit cost — omit and you get (and pay for) the 5s default. |
| `aspect_ratio` | string | — |  |
| `resolution` | string | — |  |
| `seed` | integer | — |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_audio`

**Generate voiceover** · create · also in the ChatGPT plugin

Generate speech / audio via TTS. Returns a request_id immediately (submit mode) while the job runs in the background. Hosts that show the inline media card update it by themselves; otherwise check the job with versely_get_task_status.

Each model requires a voice id from its own catalog — cross-pollinating
voices between providers (e.g. 'Adam' on Qwen, 'Ethan' on ElevenLabs) is
the most common cause of failed jobs. When voice is omitted, a safe
default is filled in for models that ship one. Invalid voices are
rejected at the MCP layer with a list of valid options.

If you need a voice ID for ANY model — especially ElevenLabs, Cartesia,
Inworld, or a non-default Minimax voice — call **versely_list_voices**
first instead of asking the user. It exposes every backend catalog with
filters (query, language, gender, accent, tags) and tells you exactly
which body field (`voice` vs `voice_id`) to pass here.

Built-in defaults / quick reference (use exactly as written, case-sensitive):
• Qwen 3 Tts 1.7b → voice: Vivian, Serena, Uncle_Fu, Dylan, Eric, Ryan, Aiden, Ono_Anna, Sohee
• Qwen 3 Tts 0.6b → voice: Vivian, Serena, Uncle_Fu, Dylan, Eric, Ryan, Aiden, Ono_Anna, Sohee
• Grok Tts → voice: eve, ara, rex, sal, leo
• Chatterbox Tts → voice: Aaron, Abigail, Anaya, Andy, Archer, Brian, Chloe, Dylan, Emmanuel, Ethan, Evelyn, Gavin, Gordon, Ivan, Laura, Lucy, Madison, Marisol, Meera, Walter
• Chatterbox Tts Turbo → voice: Aaron, Abigail, Anaya, Andy, Archer, Brian, Chloe, Dylan, Emmanuel, Ethan, Evelyn, Gavin, Gordon, Ivan, Laura, Lucy, Madison, Marisol, Meera, Walter
• Minimax Speech → voice_id: Wise_Woman, Friendly_Person, Inspirational_girl, Deep_Voice_Man, Calm_Woman, Casual_Guy, Lively_Girl, Patient_Man, Young_Knight, Determined_Man, Lovely_Girl, Decent_Boy, Imposing_Manner, Elegant_Man, Abbess, Sweet_Girl_2, Exuberant_Girl (also supports many language-specific voice IDs not listed — use versely_list_voices)

Eleven Labs Speech Turbo / Multilingual / Voice Change: voice is required and has no default. Call versely_list_voices(provider='elevenlabs', query=...) to pick one — do NOT ask the user.
Cartesia and Inworld TTS: same — fetch IDs via versely_list_voices.
Gemini 3.8 Flash TTS / Gemini 3.8 Flash Lite TTS / Gemini 3.1 Flash TTS → voice: a Gemini voice (default 'Kore'; versely_list_voices provider='gemini'). style_prompt sets the delivery (tone, pace, emotion).

Music and sound effects are not speech: use versely_generate_music and versely_generate_sound_effect.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | TTS / audio model — pass either the slug or the canonical name. Call versely_find_models with type='audio' first to discover valid identifiers. |
| `text` | string | **yes** |  |
| `voice` | string | — | Voice id for the chosen model. Per-model catalog is in the tool description; passing a voice from a different model's list (e.g. 'Ethan' on Qwen) errors at the MCP layer. Omit to use the model's default. |
| `voice_id` | string | — | Alternative voice field for models that use voice_id (e.g. Minimax Speech). Treated as equivalent to `voice` by the MCP — pass whichever feels natural. |
| `language` | string | — |  |
| `style_prompt` | string | — | Gemini TTS only: how to say it — tone, pace, emotion (e.g. 'warm and upbeat, a little faster'). |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_lipsync`

**Generate lip-sync video** · create · also in the ChatGPT plugin

Make a talking or lip-synced video with a lip-sync model from versely_find_models (type 'lipsync'); `model` is required. What the models take (versely_get_model_inputs has the details):
• A photo that speaks: image_url + audio_url (Kling Avatar, VEED Fabric, Wan 2.2 Speech, HeyGen Image to Video, LTX Audio to Video).
• A video whose mouth is re-synced to new speech: video_url + audio_url (Sync Lipsync 2.0 / Pro, Sync React 1, Kling Lipsync, VEED Lipsync).
• A stock avatar: avatar_id + audio_url, or avatar_id + script (Veed Avatars, HeyGen Avatar V3 / V5, Avatar X).
A voiceover from versely_generate_audio works as audio_url. Spends the user's Versely credits; the inline card updates itself when it finishes.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `model` | string | **yes** | Lip-sync model — required. Pass the name from versely_find_models with type='lipsync'. |
| `image_url` | string (url) | — | The face photo to animate (photo models). |
| `audio_url` | string (url) | — | The speech to sync to. |
| `video_url` | string (url) | — | The video whose mouth is re-synced (video-to-lipsync models). |
| `script` | string | — | Text to speak, for text-driven avatar models; also used as the prompt. |
| `prompt` | string | — | Scene or motion direction, for models that take one. |
| `avatar_id` | string | — | Stock avatar id (Veed Avatars, HeyGen Avatar, Avatar X). |
| `voice_id` | string | — | HeyGen voice, when the avatar speaks a script. |
| `resolution` | string | — | Output resolution, where the model offers a choice. |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_remove_background`

**Remove video background** · create · also in the ChatGPT plugin

Remove the background from a VIDEO, producing a transparent (VP9 alpha) matte for compositing. Video only — there is no image background-removal model behind this endpoint. Use 'Veed Video Background Removal Fast' for a quick pass, or the Green Screen variant for a chroma-key matte.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** | Source video to cut out. |
| `model` | enum("Veed Video Background Removal" \| "Veed Video Background Removal Fast" \| "Veed Video Background Removal Green Screen" \| "BRIO Video Background Removal") | — | Background-removal model — required by the backend; defaults to the standard VEED matte. |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_upscale_image`

**Upscale image** · create · also in the ChatGPT plugin

Upscale an image to a higher resolution.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `image_url` | string (url) | **yes** |  |
| `model` | enum("Topaz Upscale Image" \| "SeedVR Upscale" \| "Clarity Crystal Upscaler" \| "PrunaAI P-Image-Upscale") | — | Upscale model — required by the backend; defaults to Topaz. |
| `scale` | integer | — | Upscale multiplier (e.g. 2 or 4). Default 2. |
| `scale_factor` | number | — | Deprecated alias for scale — prefer scale. |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_upscale_video`

**Upscale video** · create · also in the ChatGPT plugin

Upscale a video to a higher resolution.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `model` | enum("Topaz Upscale Video" \| "Bytedance Upscaler Video" \| "Video Enhancer Pro" \| "Runway Upscale V1") | — | Upscale model — required by the backend; defaults to Topaz. |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

## Music + sound effects

_3 tools_

### `versely_generate_music`

**Generate music** · create · also in the ChatGPT plugin

Generate a song, instrumental or backing track. Models (pass as `model`; default 'Suno V6'): Suno V6, Suno V6 Mini, Suno V6 Wild, Suno V5.5, Suno V5, Lyria 3.5, Lyria 3 Pro, Lyria 3 Clip, MiniMax Music 3, ElevenLabs Music V2.5, Sonilo Text to Music. Suno: full songs; its vocals follow `prompt` (up to 500 characters) or your own `lyrics`. Lyria 3.5 / 3 Pro: full songs with vocals or instrumental, can score an `image_url`; Lyria 3 Clip: 30 seconds. MiniMax Music 3: needs `lyrics` ([verse] / [chorus] lines). ElevenLabs Music and Sonilo: set the length with duration_seconds; Sonilo can also score a video (`video_url`). Spends the user's Versely credits; the inline card updates itself when the track is ready (Lyria answers straight away).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `prompt` | string | **yes** | What the music should be: genre, mood, instruments, vocals. With Suno custom_mode it is the literal lyrics. |
| `model` | string | — | Music model (default 'Suno V6'): Suno V6, Suno V6 Mini, Suno V6 Wild, Suno V5.5, Suno V5, Lyria 3.5, Lyria 3 Pro, Lyria 3 Clip, MiniMax Music 3, ElevenLabs Music V2.5, Sonilo Text to Music. |
| `lyrics` | string | — | The words to sing. Required for MiniMax Music 3. With Suno it switches on custom mode (prompt then sets the style). |
| `instrumental` | boolean | — | No vocals (default false). |
| `duration_seconds` | number | — | Length for MiniMax (10-300 s), ElevenLabs (3-600 s) and Sonilo (5-600 s); default 60. Suno and Lyria choose their own. |
| `title` | string | — | Track title (Suno). |
| `style` | string | — | Suno custom mode: musical style, e.g. 'pop, upbeat, electronic'. |
| `custom_mode` | boolean | — | Suno: treat `prompt` as the literal lyrics (then `style` and `title` are required). |
| `negative_tags` | string | — | Suno: styles to avoid. |
| `vocal_gender` | enum("m" \| "f") | — | Suno: singer's voice. |
| `style_weight` | number | — |  |
| `weirdness_constraint` | number | — |  |
| `audio_weight` | number | — |  |
| `persona_id` | string | — |  |
| `image_url` | string (url) | — | Lyria: an image the music should fit. |
| `video_url` | string (url) | — | Sonilo: a video to score (makes Sonilo Video to Music). |
| `model_version` | string | — | Deprecated alias for model (e.g. 'V5') — prefer model. |
| `tags` | string | — | Deprecated alias for style — prefer style. |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_extend_music`

**Extend music track** · create · also in the ChatGPT plugin

Extend a Suno track (made with versely_generate_music on a Suno model) from a given timestamp. Supplying prompt / style / title / continue_at_seconds steers the continuation; omit them all to simply continue the track as it was. Spends the user's Versely credits; the inline card updates itself when it finishes.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `audio_id` | string | **yes** | Audio variant ID of the source track (NOT the task_id). |
| `model` | string | — | Suno version: V6 (default), V6_MINI, V6_WILD, V5_5 or V5. Use the source track's version when known. |
| `continue_at_seconds` | number | — | Position in seconds to continue from. |
| `prompt` | string | — |  |
| `style` | string | — |  |
| `title` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_sound_effect`

**Generate sound effect** · create · also in the ChatGPT plugin

Generate a sound effect (whoosh, footsteps, rain, crowd, UI click, ambience...) from a description, or sound effects that match a video. Models (pass as `model`): ElevenLabs Sound Effects, Suno Sounds V6, Suno Sounds V6 Mini, Suno Sounds V6 Wild, Suno Sounds V5.5, Suno Sounds V5, Sonilo Video to SFX. Default: 'ElevenLabs Sound Effects' (0.5-22 s, set duration_seconds; can loop). Suno Sounds take a tempo (bpm) and musical key and can loop. 'Sonilo Video to SFX' scores a `video_url` (used automatically when one is given). Spends the user's Versely credits; the inline card updates itself when it is ready.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `prompt` | string | — | What it should sound like. Required unless video_url is given. |
| `model` | string | — | Sound-effect model: ElevenLabs Sound Effects, Suno Sounds V6, Suno Sounds V6 Mini, Suno Sounds V6 Wild, Suno Sounds V5.5, Suno Sounds V5, Sonilo Video to SFX. |
| `duration_seconds` | number | — | ElevenLabs only: length in seconds, 0.5-22 (default 10). |
| `loop` | boolean | — | Make it loop seamlessly (ElevenLabs, Suno Sounds). |
| `prompt_influence` | number | — | ElevenLabs only: how closely to follow the prompt, 0-1 (default 0.3). |
| `bpm` | integer | — | Suno Sounds only: tempo. |
| `key` | string | — | Suno Sounds only: musical key, e.g. 'C minor'. |
| `video_url` | string (url) | — | A video to make matching sound effects for (Sonilo Video to SFX). |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

## Slideshow

_8 tools_

### `versely_create_slideshow`

**Create slideshow** · create · also in the ChatGPT plugin

Create a slideshow by generating multiple AI images from a prompt (no automation, no overlays).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `prompt` | string | **yes** |  |
| `model` | string | — | Slideshow image model (default 'Flux Pro Ultra'): a name from versely_list_slideshow_options `models` (e.g. 'Nano Banana Pro', 'GPT Image 2', 'Seedream 5.0 Pro'). Slideshows have their own list: not every model from versely_find_models works here. |
| `num_images` | integer | — | How many images to generate (default 5). Each image is charged. |
| `n` | integer | — | Deprecated alias for num_images — prefer num_images. |
| `aspect_ratio` | string | — |  |

### `versely_create_automated_slideshow`

**Create captioned slideshow** · create · also in the ChatGPT plugin

Full automation: AI plans the slideshow, generates the images, and burns text overlays in one request.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `prompt` | string | — | Topic / theme for the slideshow. Required (or pass the legacy `topic`). |
| `num_images` | integer | — | How many slides to generate (default 5, max 20). Each is charged. |
| `model` | string | — | Slideshow image model (default 'Flux Pro Ultra'): a name from versely_list_slideshow_options `models` (e.g. 'Nano Banana Pro', 'GPT Image 2', 'Seedream 5.0 Pro'). Slideshows have their own list: not every model from versely_find_models works here. |
| `aspect_ratio` | string | — |  |
| `content_type` | string | — | Output framing, e.g. 'reel' (default). |
| `style` | string | — |  |
| `theme` | string | — |  |
| `text_style` | object | — | Styling for the burned-in overlay text (font, colour, background, …). |
| `topic` | string | — | Deprecated alias for prompt — prefer prompt. |
| `n_slides` | integer | — | Deprecated alias for num_images — prefer num_images. |

### `versely_get_slideshow`

**Get slideshow** · read (read-only) · also in the ChatGPT plugin

Get a slideshow by ID, including its images.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |

### `versely_list_slideshows`

**List slideshows** · read (read-only) · also in the ChatGPT plugin

List the authenticated user's slideshows (paginated, newest first).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `limit` | integer | — | Page size (default 20). |
| `offset` | integer | — | How many to skip (default 0). Use for paging, not `page`. |

### `versely_delete_slideshow`

**Delete slideshow** · delete (destructive) · also in the ChatGPT plugin

Delete a slideshow and all its images.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |

### `versely_add_slideshow_images`

**Add slideshow images** · create · also in the ChatGPT plugin

Generate and append more AI images to an existing slideshow.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |
| `prompt` | string | **yes** |  |
| `num_images` | integer | — | How many images to append (default 3). Each image is charged. |
| `n` | integer | — | Deprecated alias for num_images — prefer num_images. |
| `model` | string | — | Slideshow image model (default 'Flux Pro Ultra'): a name from versely_list_slideshow_options `models` (e.g. 'Nano Banana Pro', 'GPT Image 2', 'Seedream 5.0 Pro'). Slideshows have their own list: not every model from versely_find_models works here. |

### `versely_add_text_overlay`

**Add text to slides** · edit (destructive) · also in the ChatGPT plugin

Burn text overlays onto a slideshow's images. Pass an array of overlays (one per slide or shared).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |
| `overlays` | object[] | **yes** |  |

### `versely_slideshow_to_video`

**Turn slideshow into video** · create · also in the ChatGPT plugin

Compile a slideshow's images (and optional audio) into an MP4 video with transitions (server-side FFmpeg).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_id` | string | **yes** |  |
| `duration_per_image` | number | — | Seconds each image is held on screen (default 3). |
| `duration_per_slide_seconds` | number | — | Deprecated alias for duration_per_image — prefer duration_per_image. |
| `transition` | string | — | Transition between slides (default 'none'). |
| `audio_url` | string (url) | — |  |
| `voiceover_url` | string (url) | — |  |
| `music_url` | string (url) | — |  |
| `output_resolution` | enum("720p" \| "1080p" \| "4k") | — | Default '1080p'. |
| `aspect_ratio` | enum("reel" \| "story" \| "post" \| "landscape" \| "portrait") | — | Named preset (default 'reel'). NOT a ratio string — '9:16' etc. will not match. |

## Movie (multi-scene)

_11 tools_

### `versely_create_movie`

**Plan a movie** · create · also in the ChatGPT plugin

Create a multi-scene movie project. Scenes are stored but NOT generated yet — call versely_generate_movie_scenes to start. Returns a blueprint card so the user can preview the plan before generation.

Field names match the backend exactly (generation_type, duration, first_frame_url, last_frame_url) — pass them through verbatim. The five generation_type values give full access to chained-scene movies where each scene picks up from the previous one's last frame.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `title` | string | **yes** |  |
| `description` | string | — |  |
| `aspect_ratio` | string | — | Default aspect ratio for all scenes (e.g. '16:9', '9:16', '1:1'). Individual scenes can override. |
| `transition_type` | enum("concat" \| "fade" \| "dissolve" \| "wipe") | — | Transition between scenes when the movie is auto-combined. Default 'concat' (hard cut). |
| `transition_duration` | number | — | Transition length in seconds (default 0.5 for fade/dissolve/wipe; ignored for concat). |
| `scenes` | object[] | **yes** |  |

### `versely_list_movies`

**List movies** · read (read-only) · also in the ChatGPT plugin

List the authenticated user's movies (paginated).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `page` | integer | — |  |
| `limit` | integer | — |  |

### `versely_get_movie`

**Get movie** · read (read-only) · also in the ChatGPT plugin

Get a movie with all its scenes and metadata. Returns a media card reflecting current status (pending / completed / failed) — same translation as versely_get_movie_status, but with the full scene metadata available to the LLM via structuredContent.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_delete_movie`

**Delete movie** · delete (destructive) · also in the ChatGPT plugin

Delete a movie and all its scenes.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_get_movie_status`

**Check movie progress** · read (read-only) · also in the ChatGPT plugin

Get real-time generation status for a movie and each of its scenes. This is the tool the iframe self-polls every ~5s during movie generation — the response is normalized into a MediaCardPayload (pending/completed/failed) so the iframe can update the card in place. The movie's card already follows progress by itself: don't call this in a loop just to show progress — only when you need the result, spaced a minute or two apart.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_generate_movie_scenes`

**Generate movie** · create · also in the ChatGPT plugin

Kick off generation for all pending scenes in a movie. Returns a pending media card immediately; the iframe self-polls versely_get_movie_status to update progress. Once all scenes are generated the backend auto-combines them into a final video and the card resolves to the playable result. The LLM does NOT need to call versely_combine_movie unless the user wants a manual re-combine with different settings.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_combine_movie`

**Recombine movie** · edit (destructive) · also in the ChatGPT plugin

Manually (re)combine a movie's scene videos into a final video with server-side FFmpeg. **You usually don't need to call this** — the backend auto-combines when all scenes complete. Use this only when (a) auto-combine failed, or (b) some scenes failed and you want to combine just the successful ones. The transition comes from the movie's stored transition_type and cannot be overridden here; there is no audio-track parameter.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

### `versely_add_movie_scene`

**Add movie scene** · create · also in the ChatGPT plugin

Add a new scene to an existing movie (appended at the end by default). The scene starts as 'pending' and won't generate until you call versely_generate_movie_scenes again — calling generate will pick up only scenes that haven't been generated yet. Field names match the backend exactly: generation_type, duration, first_frame_url, last_frame_url.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |
| `generation_type` | enum("text_to_video" \| "image_to_video" \| "first_last_frame" \| "previous_scene_image_to_video" \| "previous_scene_first_last_frame") | — | Same five values as versely_create_movie scenes. |
| `model` | string | **yes** | Video model (call versely_find_models with type='video' first). |
| `prompt` | string | — |  |
| `image_url` | string (url) | — | Required for image_to_video. |
| `first_frame_url` | string (url) | — | Required for first_last_frame. |
| `last_frame_url` | string (url) | — | Required for first_last_frame. |
| `duration` | number | — | Scene length in seconds (default 5). |
| `aspect_ratio` | string | — |  |
| `previous_scene_order` | integer | — |  |

### `versely_update_movie_scene`

**Edit movie scene** · edit (destructive) · also in the ChatGPT plugin

Update a single scene's fields (prompt, model, generation_type, image_url, etc.). Allowed only before the scene has been generated, or after a failure (use this to fix the prompt and then re-generate). Pass only the fields you want to change. Field names match the backend exactly.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `scene_id` | string | **yes** |  |
| `prompt` | string | — |  |
| `generation_type` | enum("text_to_video" \| "image_to_video" \| "first_last_frame" \| "previous_scene_image_to_video" \| "previous_scene_first_last_frame") | — |  |
| `model` | string | — | New video model for the scene (a name from versely_find_models). |
| `image_url` | string (url) | — |  |
| `first_frame_url` | string (url) | — |  |
| `last_frame_url` | string (url) | — |  |
| `duration` | number | — |  |
| `aspect_ratio` | string | — |  |

### `versely_regenerate_scene`

**Regenerate movie scene** · edit (destructive) · also in the ChatGPT plugin

Regenerate a single scene without redoing the whole movie. Use this when one scene failed or the user wants to retry just that scene with a tweaked prompt. The movie's auto-combine will fire when all scenes (including this re-run) are terminal. Returns a per-scene submission; poll versely_get_movie_status to track overall movie progress.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |
| `scene_id` | string | **yes** |  |

### `versely_cancel_movie`

**Cancel movie** · delete (destructive) · also in the ChatGPT plugin

Cancel a movie that's still generating. Marks the movie and any non-terminal scenes as 'cancelled' and preserves any scenes that already completed. Credits: scenes that never started were never charged; scenes already generating finish and keep their charge (cancelling moves no credits). Use this instead of versely_delete_movie when the user wants to keep the partial output. No-op (200) if the movie is already in a terminal status.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `movie_id` | string | **yes** |  |

## UGC (overlays + captions)

_5 tools_

### `versely_add_video_overlay`

**Overlay video** · create · also in the ChatGPT plugin

Overlay a foreground video (e.g. talking head) on top of a base video, positioned in a corner.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `slideshow_video_url` | string (url) | — | The BASE video the overlay is composited onto. Required (or pass legacy base_video_url). |
| `base_video_url` | string (url) | — | Deprecated alias for slideshow_video_url — prefer slideshow_video_url. |
| `overlay_video_url` | string (url) | **yes** | The video laid on top (e.g. a talking head). |
| `position` | enum("top-left" \| "top-right" \| "bottom-left" \| "bottom-right" \| "center") | — | Corner placement. Either this or overlay_x + overlay_y is required. |
| `overlay_x` | number | — | Free-form X, 0.0–1.0. Use WITH overlay_y. |
| `overlay_y` | number | — | Free-form Y, 0.0–1.0. Use WITH overlay_x. |
| `overlay_size` | enum("small" \| "medium" \| "large") | — | Preset overlay size (default 'medium'). Ignored if overlay_scale is set. |
| `overlay_scale` | number | — | Overlay size as a PERCENTAGE, 20–200 (not a 0–1 fraction). Takes priority over overlay_size. |
| `scale` | number | — | Deprecated: 0–1 fraction. Converted to overlay_scale percent (0.5 → 50). |
| `remove_background` | boolean | — | AI-remove the overlay's background (VEED VP9 alpha) before compositing. |
| `background_image_url` | string (url) | — |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_add_captions`

**Add caption** · create · also in the ChatGPT plugin

Add a single static caption to a video at top / middle / bottom.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `caption_text` | string | **yes** |  |
| `position` | enum("top" \| "middle" \| "bottom") | **yes** | Caption placement. Required — the backend 400s without it. |
| `font_family` | string | — | Font family (default 'Arial'). |
| `font_size` | integer | — | Default 48. |
| `font_color` | string | — | Font colour (default 'white'). |
| `font` | string | — | Deprecated alias for font_family. |
| `color` | string | — | Deprecated alias for font_color. |
| `background` | string | — | Caption backdrop (default 'solid'). |
| `outline_width` | number | — |  |
| `outline_color` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_add_timestamped_captions`

**Add timed captions** · create · also in the ChatGPT plugin

Burn CALLER-SUPPLIED timestamped captions onto a video (Reels-style). **This does NOT transcribe** — you must provide the `captions` array yourself, with explicit start/end times. To transcribe the video's audio automatically instead, use versely_add_auto_captions (Caption Studio), which also offers 30 designed presets.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `captions` | object[] | **yes** | Required, non-empty. Each entry needs start_sec, end_sec and text. |
| `caption_size` | enum("small" \| "medium" \| "large" \| "xl") | — | Default 'medium'. |
| `position` | enum("top" \| "middle" \| "bottom") | — | Default 'bottom'. |
| `font_family` | string | — | Default 'Arial'. |
| `font_color` | string | — | Default 'white'. |
| `outline_width` | number | — |  |
| `outline_color` | string | — |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_compose_with_overlay`

**Compose video** · create · also in the ChatGPT plugin

Stitch a sequence of images/videos into a base composition and optionally overlay another video on top.

`base_media` is an ordered array of objects, NOT plain URLs: `{ kind: 'image'|'video', url, duration_sec?, start_sec?, end_sec? }`. `duration_sec` is REQUIRED on every image item (how long it holds). Video items may be trimmed with start_sec/end_sec. There is no background-audio parameter on this endpoint — mux audio separately afterwards.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `base_media` | object[] | — | Ordered base timeline. Required (or pass the legacy media_urls). |
| `media_urls` | string (url)[] | — | Deprecated: plain URLs. Converted to base_media by guessing kind from the extension and applying duration_per_item_seconds to images. Prefer base_media. |
| `duration_per_item_seconds` | number | — | Only used with the deprecated media_urls form (default 3s per image). |
| `overlay_video_url` | string (url) | — |  |
| `position` | enum("top-left" \| "top-right" \| "bottom-left" \| "bottom-right" \| "center") | — | Overlay placement (default 'bottom-right'). |
| `overlay_position` | enum("top-left" \| "top-right" \| "bottom-left" \| "bottom-right" \| "center") | — | Deprecated alias for position — prefer position. |
| `overlay_size` | enum("small" \| "medium" \| "large") | — | Default 'medium'. |
| `overlay_scale` | number | — | Overlay size as a PERCENT, 20–200. Takes priority over overlay_size. |
| `output_aspect` | string | — | Output framing (default '9:16'). |
| `remove_background` | boolean | — | AI-remove the overlay's background (VEED VP9 alpha) before compositing. |
| `background_image_url` | string (url) | — |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_get_ugc`

**Get UGC video** · read (read-only) · also in the ChatGPT plugin

Get a UGC video by ID.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ugc_id` | string | **yes** |  |

## Social media

_10 tools_

### `versely_get_social_auth_url`

**Connect social account** · create (open-world) · also in the ChatGPT plugin

Get a link the user opens to connect one of their social accounts (Instagram, TikTok, YouTube, X, Facebook, LinkedIn, Pinterest, Threads, Bluesky) to Versely, so Versely can post there. Show the link to the user as a clickable link. After they say they finished connecting, call versely_refresh_social_accounts, then versely_list_social_accounts to get the new account's id.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `platform` | enum("instagram" \| "tiktok" \| "youtube" \| "x" \| "twitter" \| "facebook" \| "linkedin" \| "pinterest" \| "threads" \| "bluesky") | **yes** | Which platform to connect. |
| `redirect_url` | string (url) | — | Where to send the user after they connect. Defaults to Versely's accounts page. |

### `versely_list_social_accounts`

**List social accounts** · read (read-only) · also in the ChatGPT plugin

List the social accounts the user connected to Versely. Each account's `id` is what versely_publish_post and versely_preview_post take in account_ids. If an account the user just connected is missing, call versely_refresh_social_accounts first.

_No input parameters._

### `versely_refresh_social_accounts`

**Refresh social accounts** · edit (open-world) · also in the ChatGPT plugin

Link social accounts the user just connected through a connect link to their Versely account, and update the names and pictures of the ones already connected. Call it after the user finishes a connect link.

_No input parameters._

### `versely_disconnect_social_account`

**Disconnect social account** · delete (destructive, open-world) · also in the ChatGPT plugin

Disconnect one of the user's social accounts from Versely (by its `id` from versely_list_social_accounts). Scheduled posts to it will fail. It can be connected again later with a new connect link.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `account_id` | string | **yes** | The account's `id` from versely_list_social_accounts. |

### `versely_preview_post`

**Preview social post** · read (read-only) · also in the ChatGPT plugin

Preview how a post will look on each chosen account, without publishing or charging credits. Needs a caption or at least one media URL.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `caption` | string | — | Required unless media_urls is provided. |
| `media_urls` | string (url)[] | — | Public https URLs of the images or video to post. |
| `account_ids` | string[] | — | Connected account ids to preview for. Required — from versely_list_social_accounts. |
| `tiktok_draft` | boolean | — | Preview TikTok as a draft sent to the TikTok app. |
| `targets` | object[] | — | Deprecated shape — prefer account_ids. |

### `versely_publish_post`

**Publish social post** · publish (open-world) · also in the ChatGPT plugin

Publish a post (caption plus images or a video) to one or more of the user's connected social accounts, now or at a scheduled time. Confirm the caption, media and accounts with the user first. Charges Versely credits per account posted to (currently 1 each; the result says how many). Media rules: YouTube takes video only; TikTok takes one video or pictures, never both; Instagram needs an image or a video; X and Bluesky post at most 4 pictures. A scheduled post can be moved with versely_update_post, or cancelled for a refund with versely_delete_post before it goes out. Follow up with versely_get_post for the live links.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `caption` | string | **yes** | Post caption. Required. |
| `media_urls` | string (url)[] | — | Public https URLs of the images or video to post (Versely URLs from earlier results work). |
| `account_ids` | string[] | — | Connected account ids to publish to. Required — from versely_list_social_accounts. |
| `scheduled_at` | string | — | Future ISO-8601 time to publish at, with a timezone offset. Omit to publish now. |
| `is_draft` | boolean | — | Save as a draft in Versely instead of publishing (no charge). |
| `tiktok_draft` | boolean | — | Send TikTok posts to the TikTok app's drafts for the user to finish there, instead of posting live. |
| `targets` | object[] | — | Deprecated shape — prefer account_ids. |

### `versely_list_posts`

**List social posts** · read (read-only) · also in the ChatGPT plugin

List the user's social posts, newest first, with their status (draft, scheduled, processing, posted, partial, failed). Note: `platform` is filtered within the fetched page only — widen `limit` if you filter.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `limit` | integer | — | Page size (default 20). |
| `offset` | integer | — | How many to skip (default 0). Use for paging, not `page`. |
| `platform` | enum("instagram" \| "tiktok" \| "youtube" \| "x" \| "twitter" \| "facebook" \| "linkedin" \| "pinterest" \| "threads" \| "bluesky" \| "tiktok_business") | — | Optional filter on the returned page. |

### `versely_get_post`

**Get social post** · read (read-only) · also in the ChatGPT plugin

Get one of the user's social posts: its status, per-platform results and the live links once published (`post_urls`).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `post_id` | string | **yes** | The post's id from versely_list_posts. |

### `versely_update_post`

**Reschedule social post** · edit (destructive, open-world) · also in the ChatGPT plugin

Move a scheduled post to a new time and/or change its caption, without charging again. Only posts that are still scheduled (or drafts, caption only) can be changed.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `post_id` | string | **yes** | The post's id from versely_list_posts. |
| `scheduled_at` | string | — | New future ISO-8601 time, with a timezone offset. |
| `caption` | string | — | New caption. |

### `versely_delete_post`

**Delete social post** · delete (destructive, open-world) · also in the ChatGPT plugin

Delete one of the user's social posts from Versely. A post that is still scheduled is cancelled before it goes out and its credits are refunded (`refunded`). A post that was already published stays live on the platform.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `post_id` | string | **yes** | The post's id from versely_list_posts. |

## Status / async polling

_2 tools_

### `versely_get_task_status`

**Check job progress** · read (read-only) · also in the ChatGPT plugin

Get the current status of an async generation task by request_id (single, non-blocking lookup). NOTE: on hosts that render inline media cards, the generation tool's own card already polls and displays the finished media BY ITSELF — do not call this in a rapid loop just to show the user progress. Call it when you actually need the asset URLs (e.g. as reference input for a next step), and space calls out by ~15-30s.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `request_id` | string | **yes** | The request_id / task_id returned by a generation tool. |

### `versely_wait_for_task`

**Wait for job** · read (read-only)

Block and poll a request_id until it finishes — but for at most ~70 seconds, regardless of what poll_timeout_ms says. If the job is still running when that budget runs out this returns status 'still_running' (NOT an error, and the job is unaffected). For anything slower than about a minute — video especially — prefer calling versely_get_task_status a few times instead of blocking here.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `request_id` | string | **yes** | The request_id / task_id to wait on. |
| `poll_timeout_ms` | integer | — | Requested poll timeout in ms. CLAMPED to ~70s — a longer block is severed by the proxy in front of this server and surfaces as 'unable to reach the server', so a bigger number here buys nothing. |
| `poll_interval_ms` | integer | — | Override default poll interval in ms. |

## Utilities

_6 tools_

### `versely_extract_frames`

**Extract video frames** · create · also in the ChatGPT plugin

Extract frames from a video, evenly spaced across a time range. Note: the backend extracts N frames across a [start, end] window — it cannot extract an arbitrary list of individual timestamps.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** |  |
| `start_seconds` | number | — | Start of the extraction window, in seconds (default 0). |
| `end_seconds` | number | — | End of the extraction window, in seconds. Defaults to 1s after start. |
| `frame_count` | integer | — | How many frames to extract, evenly spaced across the window (default 1). |
| `count` | integer | — | Deprecated alias for frame_count — prefer frame_count. |
| `fps` | number | — | Sampling rate (default 1). |
| `output_format` | enum("png" \| "jpg" \| "jpeg") | — | Frame image format (default 'png'). |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_merge_videos`

**Merge videos** · create · also in the ChatGPT plugin

Merge multiple videos into a single video (server-side FFmpeg).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_urls` | string (url)[] | **yes** |  |
| `transition_type` | enum("concat" \| "fade" \| "dissolve" \| "wipe") | — | Transition between clips (default 'concat' = hard cut). |
| `transition` | string | — | Deprecated alias for transition_type — prefer transition_type. |
| `transition_duration` | number | — | Transition length in seconds (default 0.5). Ignored for 'concat'. |
| `width` | integer | — |  |
| `height` | integer | — |  |
| `fps` | number | — |  |
| `maintain_aspect_ratio` | boolean | — |  |
| `audio_handling` | string | — | How to handle source audio (default 'merge'). |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_generate_prompt`

**Improve a prompt** · create

Use Gemini to expand a brief idea into a richer generation prompt. Costs 1 Versely credit; starts no media generation.

`content_type: 'speech'` switches to the spoken-delivery enhancer, which rewrites the text for TTS (pacing, emphasis, pronunciation) and returns { enhanced_text, style_suggestion?, provider_scheme? }. Pair it with target_provider/mood/language before calling versely_generate_audio.

Only 'image', 'video' and 'speech' are supported — there is no music prompt enhancer.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `description` | string | — | Short concept to expand into a full prompt. Required (or pass the legacy `idea`). |
| `content_type` | enum("image" \| "video" \| "speech") | — | What the prompt is for (default 'image'). 'speech' uses the TTS-delivery enhancer. |
| `style` | string | — |  |
| `model` | string | — | Target model, to tailor the prompt's phrasing. |
| `aspect_ratio` | string | — |  |
| `creativity` | enum("low" \| "medium" \| "high") | — | Default 'medium'. |
| `include_negative` | boolean | — | Also return a negative prompt. |
| `target_provider` | enum("elevenlabs" \| "chatterbox" \| "grok" \| "inworld" \| "cartesia" \| "gemini" \| "qwen3" \| "minimax") | — | speech only — tailors the output to that TTS provider's markup scheme. |
| `mood` | string | — | speech only — e.g. 'excited', 'somber'. |
| `language` | string | — | speech only — target language. |
| `idea` | string | — | Deprecated alias for description — prefer description. |
| `target` | enum("image" \| "video" \| "music" \| "any") | — | Deprecated alias for content_type — prefer content_type. |

### `versely_colorize_photo`

**Colorize photo** · create · also in the ChatGPT plugin

Colorize a grayscale or faded photo.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `image_url` | string (url) | **yes** |  |
| `saturation` | number | — | Colour intensity, 0–1. Must be a number if provided. |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_audio_isolation`

**Isolate voice** · create · also in the ChatGPT plugin

Isolate vocals (or remove background noise) from an audio clip.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `audio_url` | string (url) | **yes** |  |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

### `versely_add_auto_captions`

**Auto-caption video** · create · also in the ChatGPT plugin

Caption Studio — automatically transcribe a video's speech and burn in styled captions. Unlike versely_add_captions (one fixed line) and versely_add_timestamped_captions (you supply the timings), this transcribes the audio itself. Can also translate the captions into another language, or render a caption track you already have as SRT.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `video_url` | string (url) | **yes** | The video to caption. |
| `preset` | enum("glass" \| "whisper" \| "glide2" \| "fusion" \| "glide" \| "terminal" \| "handwritten" \| "backdrop" \| "backdrop2" \| "simple" \| "plain" \| "beans" \| "corpo" \| "boo" \| "shadeplay" \| "casper" \| "capri" \| "lowkey" \| "vinta" \| "diego" \| "ali" \| "slay" \| "kitty" \| "hustle" \| "karl" \| "sprout" \| "flex" \| "mint" \| "rizz" \| "vegas") | **yes** | Caption design. Required. Animated word-by-word presets (billed at 2x): glass, whisper, glide2, fusion, glide, terminal, handwritten, backdrop, backdrop2. Standard presets: simple, plain, beans, corpo, boo, shadeplay, casper, capri, lowkey, vinta, diego, ali, slay, kitty, hustle, karl, sprout, flex, mint, rizz, vegas. 'glass' is a good default for social video. |
| `language` | string | — | Language of the SPOKEN audio, as a locale tag like 'en-US', 'es-ES' or 'hi-IN' — note the region is required here, unlike translation_language. Omit to auto-detect. |
| `translation_language` | string | — | Translate the captions into this language, e.g. 'es' or 'pt-BR'. The spoken audio is unchanged — only the subtitles are translated. To translate the actual speech, use versely_create_dub instead. |
| `vocabulary` | string[] | — | Up to 50 proper nouns, brand names or jargon to bias the transcription toward. Use this when a name is being misheard. |
| `srt_content` | string | — | Render this SRT instead of transcribing. Skips speech recognition entirely. |
| `srt_file_url` | string (url) | — | Same as srt_content, but fetched from a URL. |
| `position` | enum("top" \| "center" \| "bottom") | — | Caption placement. |
| `shadow` | enum("none" \| "min" \| "mid" \| "max") | — | Drop-shadow strength. |
| `font` | string | — | Font family override. |
| `weight` | integer | — | Font weight, 100–900 in steps of 100. |
| `color` | string | — | Text colour. Hex only — named colours are rejected. |
| `mode` | enum("wait" \| "submit") | — | 'submit' (default) returns the request_id immediately — check it with versely_get_task_status, and check again if it's still pending. 'wait' holds this tool call open while polling, but for at most ~70s: past that the proxy in front of this server cuts the connection and the caller sees 'unable to reach the server' rather than a result. Only use 'wait' for jobs you expect to finish inside a minute; anything longer (video especially) should submit and poll. |
| `poll_timeout_ms` | integer | — | Override default poll timeout in ms (only used when mode='wait'). |
| `poll_interval_ms` | integer | — | Override default poll interval in ms (only used when mode='wait'). |

## Workflows

_25 tools_

### `versely_create_workflow`

**Create workflow** · create · also in the ChatGPT plugin

Create a new user workflow. Provide either `steps` (legacy multi-step mode) or `scenes` (newer scene-graph mode) — at least one must be populated.

**Scene-graph prerequisite chain:**
1. Create reusable assets first with `versely_create_workflow_asset` (one per character / product / brand kit). OR call `versely_prepare_workflow_assets` to do steps 1 + 2 in a single call.
2. Reference them in `asset_map` as `{ "<key>": { asset_id, asset_type, name } }` — the key is the slot name scenes will use.
3. In each scene, list the slot keys you want in `reference_image_keys`. Every key MUST exist in `asset_map` (backend rejects unknown keys with a 400). If omitted, the backend auto-injects ALL asset_map keys for that scene so character identity stays stable.

For multi-scene runs, also set `style_preamble` + `voice_preamble` so visual style and narrator persona stay consistent across independently-rendered scenes.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | string | **yes** | Display name (required). |
| `description` | string | — |  |
| `icon` | string | — | Icon name; defaults to 'git-branch-outline'. |
| `prompt` | string | — | Free-form instruction for the workflow. |
| `steps` | any[] | — | Legacy mode: ordered array of step objects (each with action/inputs). Mutually exclusive with `scenes` in practice. |
| `asset_map` | object | — | Scene-graph mode: map of asset slot key → { asset_id, asset_type, name }. The key is what scenes reference in `reference_image_keys`. All three fields are required per entry (backend rejects missing asset_type / name with a 400). Example: { "main_character": { asset_id: "...uuid...", asset_type: "character", name: "Main character" } }. |
| `scenes` | object[] | — | Scene-graph mode: ordered scenes. Each scene needs `order` (1-based, unique), `img_prompt`, `video_prompt`. Optional: `reference_image_keys` (must all exist in asset_map), `voiceover`, `duration`, `chain_from_previous`. Scenes auto-sort by `order` on save. |
| `aspect_ratio` | string | — | e.g. '9:16' (default), '1:1', '16:9'. |
| `style_preamble` | string | — | Prepended to every scene's image prompt at run time. Use to lock visual style across the whole workflow (e.g. 'warm 35mm film grain, soft natural light, shallow depth of field'). Cheap insurance against scene-to-scene style drift when scenes are rendered independently. |
| `voice_preamble` | string | — | Prepended to every scene's voiceover as a 'Voice direction:' header at run time. Use to lock narrator persona / tone / pacing across scenes (e.g. 'calm female narrator, mid-30s, conversational pacing, slight smile'). Only applies to scenes that have a `voiceover` field — has no effect otherwise. |

### `versely_list_workflows`

**List workflows** · read (read-only) · also in the ChatGPT plugin

List workflows owned by the authenticated user, newest-updated first. Returns `total` alongside the page — page with `limit`/`offset` if `total` exceeds what you received.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `limit` | integer | — | Page size (default 20, server caps at 50). |
| `offset` | integer | — | How many to skip (default 0). |

### `versely_get_workflow`

**Get workflow** · read (read-only) · also in the ChatGPT plugin

Fetch a single workflow by ID (definition, scenes, asset_map, schedule state). The web-canvas graph (`node_ui`) is summarised out by default — it can run to hundreds of nodes with run media painted onto it, and is not needed to read or edit a workflow. Pass include_node_ui only if you specifically need the canvas layout. For run health/history, versely_summarize_workflow is cheaper.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `include_node_ui` | boolean | — | Return the full canvas graph instead of a summary. Large — off by default. |

### `versely_update_workflow`

**Edit workflow** · edit (destructive) · also in the ChatGPT plugin

Patch core workflow fields (name, description, icon, prompt, steps, scenes, asset_map, aspect_ratio, preambles). Only fields you provide are updated. **If you touch scenes or asset_map, send them as a pair** — the backend revalidates `scenes[].reference_image_keys` against `asset_map`, so updating only one side can fail if keys drift.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `name` | string | — |  |
| `description` | string | — |  |
| `icon` | string | — |  |
| `prompt` | string | — |  |
| `steps` | any[] | — |  |
| `asset_map` | object | — | Same shape as versely_create_workflow: Record<slot_key, { asset_id, asset_type, name }>. Send alongside `scenes` if you're changing referenced keys. |
| `scenes` | object[] | — | Same shape as versely_create_workflow scenes. Send alongside `asset_map` if you're changing referenced keys. |
| `aspect_ratio` | string | — |  |
| `style_preamble` | string | — | Prepended to every scene's image prompt at run time. Use to lock visual style across the workflow. |
| `voice_preamble` | string | — | Prepended to every scene's voiceover as a 'Voice direction:' header. Locks narrator persona; only applies when scenes have a `voiceover` field. |

### `versely_delete_workflow`

**Delete workflow** · delete (destructive) · also in the ChatGPT plugin

Delete a workflow by ID.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |

### `versely_duplicate_workflow`

**Duplicate workflow** · create · also in the ChatGPT plugin

Clone an existing workflow into a new one owned by the same user.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Source workflow UUID. |

### `versely_export_workflow`

**Export workflow** · read (read-only) · also in the ChatGPT plugin

Export a workflow as a portable JSON payload (for backup or import elsewhere).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |

### `versely_update_workflow_mode`

**Set workflow mode** · edit (destructive, open-world) · also in the ChatGPT plugin

Switch a workflow between manual and auto modes. auto_post fields enable automatic publishing on each run.

**mode='auto' has three hard preconditions, all enforced with a 400:**
1. `schedule_cron` — a 5-field cron expression.
2. The workflow must have at least one scene (scene workflows only; a steps-only workflow cannot go auto).
3. A theme anchor — the workflow needs a non-empty `description`, OR you must pass `series_premise` (either on this call or already stored). Without one, the generator has nothing to vary each run.

Check the workflow with versely_get_workflow first if you're unsure it has scenes and a description.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `mode` | enum("manual" \| "auto") | **yes** | 'manual' disables scheduling; 'auto' enables it. |
| `schedule_cron` | string | — | Required when mode='auto'. 5-field cron (minute hour dom month dow). |
| `schedule_label` | string | — |  |
| `series_premise` | string | — | Theme anchor for recurring runs. Required for mode='auto' unless the workflow already has a non-empty description. |
| `auto_post` | boolean | — | Publish each finished run to the user's connected social accounts (charges per account). |
| `auto_post_platforms` | string[] | — | Post to every connected account on these platforms (instagram, tiktok, youtube, x, ...). |
| `auto_post_account_ids` | string[] | — | Specific accounts to post to: ids from versely_list_social_accounts. Wins over auto_post_platforms. |
| `auto_post_caption_prompt` | string | — | Guidance for the auto-written post caption. |

### `versely_update_workflow_schedule`

**Set workflow schedule** · edit (destructive) · also in the ChatGPT plugin

Update the recurring schedule (cron) of an auto-mode workflow without flipping mode. Use versely_update_workflow_mode to toggle manual ↔ auto.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `schedule_cron` | string | **yes** | 5-field cron expression. |

### `versely_update_workflow_dates`

**Set workflow run dates** · edit (destructive) · also in the ChatGPT plugin

Update the explicit list of one-off scheduled run dates for a workflow (separate from the recurring cron schedule).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `scheduled_dates` | any[] | **yes** | Entries are OBJECTS: { date: 'YYYY-MM-DD', time?: 'HH:MM', event_label?: string }. A bare string is accepted and converted. |

### `versely_update_workflow_assets`

**Set workflow media** · edit (destructive) · also in the ChatGPT plugin

Set the media_assets array on a step-mode workflow (legacy). Each item is { url, type: 'image' | 'video' }. NOT for scene workflows — those use the workflow_assets table; the backend rejects this call on scene workflows with error 'scene_workflow_uses_workflow_assets'. To attach images to a scene workflow, use versely_create_workflow_asset / versely_add_workflow_asset_images.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `media_assets` | object[] | **yes** | Array of { url, type } objects. type must be 'image' or 'video'. Pass [] to clear. |

### `versely_run_workflow`

**Run workflow** · publish (open-world) · also in the ChatGPT plugin

Execute a workflow now. Returns a pending media card immediately and the iframe self-polls versely_get_workflow_run every 5s — scenes pop into the card as they complete, the backend auto-combines when all are done, and the final video appears in place.

**Two things this cannot do:**
• Canvas workflows (`workflow_kind: 'canvas'`) have no server-side scenes or steps and are rejected — they must be run from the web canvas.
• `scheduled_at` only works on legacy STEPS workflows. Scene workflows reject it; use versely_update_workflow_mode with a cron to schedule those.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `scheduled_at` | string | — | ISO-8601 timestamp. STEPS workflows only — scene workflows 400 on this. To schedule a scene workflow, use versely_update_workflow_mode (cron) instead. |

### `versely_list_workflow_runs`

**List workflow runs** · read (read-only) · also in the ChatGPT plugin

List runs (history) for a given workflow. Optional filters are applied MCP-side after the backend returns up to `limit` rows: status (canonical bucket 'active' | 'failed' | 'succeeded' or a literal value like 'running'), and since/until date range on created_at.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `status` | any | — | Filter by status. Use 'active'/'failed'/'succeeded' for a canonical bucket, or a literal value (e.g. 'running', 'generating_image'). Pass an array to OR multiple values. |
| `since` | string | — | ISO-8601 timestamp. Only return runs created at or after this time. |
| `until` | string | — | ISO-8601 timestamp. Only return runs created at or before this time. |
| `limit` | integer | — | Backend max is 50. Increase if filters are likely to drop most rows from a smaller window. |

### `versely_get_workflow_run`

**Check workflow run** · read (read-only) · also in the ChatGPT plugin

Fetch a single workflow run by its run/task ID. Unified across scenes-mode and steps-mode runs. Response is translated into a MediaCardPayload (pending / completed / failed) so the iframe poll loop can update the card in place — this is the tool the iframe self-polls during a run.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run_id` | string | **yes** | Run/task ID returned from versely_run_workflow (also called task_id). |

### `versely_list_active_workflow_runs`

**List running workflows** · read (read-only) · also in the ChatGPT plugin

Fan-out monitoring: lists every non-terminal run across ALL of the user's workflows. Useful for 'what's running right now?'. Internally lists workflows and queries each workflow's runs; cost is O(N) backend hits where N = number of workflows.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `limit_per_workflow` | integer | — | How many recent runs to inspect per workflow (default 10). Increase if some workflows have many recent terminal runs masking active ones. |

### `versely_list_failed_workflow_runs`

**List failed workflow runs** · read (read-only) · also in the ChatGPT plugin

Fan-out monitoring: lists failed/cancelled runs across ALL of the user's workflows, optionally restricted to a recent time window. Useful for 'what broke?'.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `since` | string | — | ISO-8601 timestamp. Defaults to 7 days ago. Set to '1970-01-01T00:00:00Z' for unlimited history. |
| `until` | string | — | ISO-8601 timestamp. Defaults to now. |
| `limit_per_workflow` | integer | — | How many recent runs to inspect per workflow (default 20). |

### `versely_summarize_workflow`

**Summarize workflow** · read (read-only) · also in the ChatGPT plugin

Per-workflow health summary: aggregates runs into {total, succeeded, failed, active, last_run_at, last_status, avg_duration_ms} plus the workflow's current schedule state. Cheap (one /workflows/:id and one /workflows/:id/runs call).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflow_id` | string | **yes** | Workflow UUID. |
| `since` | string | — | ISO-8601 timestamp. Only count runs created on or after this time. |
| `limit` | integer | — | How many recent runs to consider (default 50, the backend max). |

### `versely_list_scheduled_workflows`

**List scheduled workflows** · read (read-only) · also in the ChatGPT plugin

List workflows in auto mode (schedule_active=true), sorted by next_run_at ascending so the soonest-firing comes first. Useful for confirming that a workflow you set to auto is actually armed.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `include_inactive` | boolean | — | If true, also include workflows with a schedule_cron set but schedule_active=false. Default false. |

### `versely_create_workflow_asset`

**Create workflow asset** · create · also in the ChatGPT plugin

Create a reusable workflow asset (e.g. character reference, brand kit, product imagery). Can be bound to a specific workflow or left global to the user.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `asset_type` | string | **yes** | Asset category (e.g. 'character', 'product', 'brand_kit'). |
| `name` | string | **yes** | Display name. |
| `description` | string | — |  |
| `reference_images` | object[] | — | Reference images as objects: [{ url, label?, created_at? }]. NOT bare URL strings. |
| `metadata` | object | — |  |
| `workflow_id` | string | — | Bind to a specific workflow; omit for a user-global asset. |
| `slideshow_schedule_id` | string | — |  |

### `versely_list_workflow_assets`

**List workflow assets** · read (read-only) · also in the ChatGPT plugin

List the user's workflow assets, optionally filtered by asset_type or scoped to a workflow / slideshow schedule.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `asset_type` | string | — |  |
| `workflow_id` | string | — | Pass 'none' to filter to user-global assets only; an ID returns assets bound to that workflow plus user-globals. |
| `slideshow_schedule_id` | string | — |  |
| `limit` | integer | — | Default 50. |
| `offset` | integer | — |  |

### `versely_get_workflow_asset`

**Get workflow asset** · read (read-only) · also in the ChatGPT plugin

Fetch a single workflow asset by ID.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `asset_id` | string | **yes** | Workflow asset UUID. |

### `versely_update_workflow_asset`

**Edit workflow asset** · edit (destructive) · also in the ChatGPT plugin

Patch a workflow asset's mutable fields (asset_type, name, description, reference_images, metadata).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `asset_id` | string | **yes** | Workflow asset UUID. |
| `asset_type` | string | — |  |
| `name` | string | — |  |
| `description` | string | — |  |
| `reference_images` | object[] | — | Replaces the asset's full reference_images list. Objects: [{ url, label?, created_at? }]. |
| `metadata` | object | — |  |

### `versely_add_workflow_asset_images`

**Add workflow asset images** · create · also in the ChatGPT plugin

Append additional reference images to an existing workflow asset (preserves existing images).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `asset_id` | string | **yes** | Workflow asset UUID. |
| `reference_images` | object[] | **yes** | Images to append as objects: [{ url, label?, created_at? }]. NOT bare URL strings. |

### `versely_delete_workflow_asset`

**Delete workflow asset** · delete (destructive) · also in the ChatGPT plugin

Delete a workflow asset by ID.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `asset_id` | string | **yes** | Workflow asset UUID. |

### `versely_prepare_workflow_assets`

**Prepare workflow assets** · create · also in the ChatGPT plugin

Batch-create reusable workflow assets and return a ready-to-use `asset_map`. Saves you the N round-trips of calling versely_create_workflow_asset once per asset, plus the manual stitching of {asset_id, asset_type, name} into a Record.

Usage:
1. Call this with one entry per asset slot you want (each entry needs `key`, `name`, `asset_type`, `image_urls`).
2. Take the returned `asset_map` and pass it verbatim to versely_create_workflow / versely_update_workflow.
3. Reference the keys you chose in `scenes[].reference_image_keys`.

Fail-fast: if any asset fails to create, the call stops and returns the assets it has already created so far in `created` plus the error in `failed_at_index`. The successful assets are real — they exist in the user's library and can be deleted or reused via versely_list_workflow_assets if needed.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `assets` | object[] | **yes** | One entry per asset slot. Order is preserved in the response. |
| `workflow_id` | string | — | Optional: bind all created assets to a specific workflow. Omit to make them user-global (reusable across workflows). |

## Video-workflow runs

_5 tools_

### `versely_list_video_workflow_runs`

**List video workflow runs** · read (read-only) · also in the ChatGPT plugin

List the authenticated user's video-workflow runs. `status` is forwarded to the backend (server-side filter). `since`/`until` filter MCP-side on created_at after the page is fetched.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `status` | string | — | Literal backend status (e.g. 'queued', 'running', 'completed', 'failed', 'cancelled'). Forwarded to the backend's ?status= filter. |
| `since` | string | — | ISO-8601 timestamp. Only return runs created at or after this time. |
| `until` | string | — | ISO-8601 timestamp. Only return runs created at or before this time. |
| `limit` | integer | — | Page size (default 20, server caps at 50). |
| `offset` | integer | — |  |

### `versely_get_video_workflow_run`

**Check video workflow run** · read (read-only) · also in the ChatGPT plugin

Fetch a video-workflow run's status, including per-scene progress. Response is translated into a MediaCardPayload (pending / completed / failed) so the iframe poll loop can update the card in place — this is the tool the iframe self-polls during a run.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run_id` | string | **yes** | Run UUID. |

### `versely_cancel_video_workflow_run`

**Cancel video workflow run** · delete (destructive) · also in the ChatGPT plugin

Cancel an in-progress video-workflow run. Marks the run as 'cancelled' so the iframe poll loop stops; keeps any scenes that already completed.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run_id` | string | **yes** | Run UUID. |
| `reason` | string | — | Optional reason recorded with the cancellation. |

### `versely_combine_video_workflow_run`

**Recombine video workflow run** · edit (destructive) · also in the ChatGPT plugin

Manually (re)combine a video-workflow run's rendered scenes into a final video. **You usually don't need to call this** — the backend auto-combines when all scenes complete. Use this only when (a) auto-combine errored, (b) you want a different transition or audio, or (c) some scenes failed and you want to combine just the successful ones.

Returns a pending media card; combine runs server-side and the iframe poll loop picks up the final_video_url when it lands.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run_id` | string | **yes** | Run UUID (all scenes must be complete). |

### `versely_retry_video_workflow_scene`

**Retry video workflow scene** · edit (destructive) · also in the ChatGPT plugin

Retry a single failed scene within a video-workflow run, identified by its scene order. The backend resets that scene plus any downstream scenes that depended on it and re-dispatches them. Returns a pending media card; the iframe polls until the retried scenes finish (and auto-combine fires).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run_id` | string | **yes** | Run UUID. |
| `scene_order` | integer | **yes** | 1-based scene index within the run (matches scene_order from versely_get_video_workflow_run). |

## Voices

_1 tools_

### `versely_list_voices`

**List voices** · read (read-only) · also in the ChatGPT plugin

List available TTS voices for any supported provider. Two use cases:
  1. Resolving a voice ID before a TTS call — never ask the user for one;
     call this with a query/filter, pick a voice, pass its `id` to versely_generate_audio.
  2. Answering 'what voices do you have?' / 'show me british female voices' /
     'what languages does Inworld support?' — call with the matching provider and
     filters, then summarize results for the user.

CRITICAL — picking a voice from the response:
  • Pass the voice's `id` field as the value (NOT `name`). e.g. for ElevenLabs
    'Margot' { id: 'XB0fDUnXU5powFXDhCwa', name: 'Margot' } you pass
    `voice: 'XB0fDUnXU5powFXDhCwa'` — passing 'Margot' will fail with no taskId.
  • The response's `voice_field` tells you the body field name to use
    (`voice` vs `voice_id`). Do not guess.

Provider → audio model(s) it serves + the body field name to pass:
  • elevenlabs → Eleven Labs Speech Turbo, Eleven Labs Multilingual (body field: `voice`)
  • elevenlabs-library → (discovery only — not callable via versely_generate_audio; use the `elevenlabs` provider instead) (body field: `voice`)
  • elevenlabs-saved → (discovery only — voice samples cached in the user bucket) (body field: `voice`)
  • cartesia → Cartesia (direct endpoint — not yet wired into versely_generate_audio) (body field: `voice_id`)
  • cartesia-mine → Cartesia (direct endpoint — not yet wired into versely_generate_audio) (body field: `voice_id`)
  • inworld → Inworld TTS (direct endpoint — not yet wired into versely_generate_audio) (body field: `voice_id`)
  • inworld-mine → Inworld TTS (direct endpoint — not yet wired into versely_generate_audio) (body field: `voice_id`)
  • minimax → Minimax Speech (body field: `voice_id`)
  • gemini → Gemini 3.8 Flash TTS, Gemini 3.8 Flash Lite TTS, Gemini 3.1 Flash TTS (body field: `voice`)
  • suno → (discovery only — Suno has no TTS voices; use versely_generate_music for music, or another provider for speech) (body field: `voice`)
  • qwen → Qwen 3 TTS 1.7B, Qwen 3 TTS 0.6B (body field: `voice`)
  • grok → Grok TTS (body field: `voice`)
  • chatterbox → Chatterbox TTS, Chatterbox TTS Turbo (body field: `voice`)

Use `query` for substring search (matches id, name, description, gender, language, accent, tags).
Backend libraries (elevenlabs ~5000, cartesia ~1500, inworld ~70) are paginated via `limit`/`offset`.
Response includes a `facets` block listing the available filter values (languages, genders, accents, tags) so you can refine without guessing.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `provider` | enum("elevenlabs" \| "elevenlabs-library" \| "elevenlabs-saved" \| "cartesia" \| "cartesia-mine" \| "inworld" \| "inworld-mine" \| "minimax" \| "gemini" \| "suno" \| "qwen" \| "grok" \| "chatterbox") | **yes** |  |
| `query` | string | — | Case-insensitive substring filter across id, name, description, gender, language, accent, tags. |
| `language` | string | — | Filter by language (case-insensitive). |
| `gender` | string | — | Filter by gender (e.g. 'male', 'female'). |
| `accent` | string | — | Filter by accent (ElevenLabs only). |
| `tag` | string | — | Filter by tag (Inworld only). |
| `limit` | integer | — | Max voices to return. Default 25, max 100. |
| `offset` | integer | — | Pagination offset. Default 0. |
| `refresh` | boolean | — | Bust the per-process cache for dynamic providers and re-fetch. |

## Dubbing

_4 tools_

### `versely_create_dub`

**Dub video or audio** · create · also in the ChatGPT plugin

Dub a Versely-hosted video or audio file into another language (Dubbing Studio). Two engines: 'elevenlabs' (default) clones the speaker's voice, handles audio OR video, supports trimming and background separation, up to 30 minutes; 'heygen' additionally LIP-SYNCS the speaker to the new language but is video-only, has no trimming, is limited to 8 minutes, and costs ~3x more. Returns a project_id — check it with versely_get_dub (the generic task-status tools cannot see dubbing projects).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_url` | string (url) | **yes** | The media to dub. Must be Versely-hosted (a *.versely.studio URL, e.g. output from a generation tool or versely_list_user_media). External links such as YouTube are rejected — the duration probe that prices the job needs our own file. |
| `target_lang` | string | **yes** | Language to dub INTO, e.g. 'es', 'hi', 'pt-BR'. Required. |
| `source_lang` | string | — | Language of the source. Defaults to 'auto' (detected). |
| `engine` | enum("elevenlabs" \| "heygen") | — | Dubbing engine. Default 'elevenlabs'. 'heygen' adds lip-sync but requires a VIDEO source, rejects start_time/end_time, caps at 8 minutes, and only supports these target_lang values: en, es, fr, hi, it, de, pl, pt, pt-br, zh, ja, nl, tr, ko, da, ar, ro, fil, sv, id, uk, el, cs, bg, ms, sk, hr, ta, fi, ru, th, vi, he, hu, no. |
| `name` | string | — | Project name. Defaults to 'Dub → <target_lang>'. |
| `num_speakers` | integer | — | Number of distinct speakers. 0 (default) auto-detects. |
| `start_time` | integer | — | Trim start, in SECONDS. Billing is based on the selected window, so trimming a long source is how you control cost. Not supported by the heygen engine. |
| `end_time` | integer | — | Trim end, in SECONDS. Must be greater than start_time. Not supported by the heygen engine. |
| `drop_background_audio` | boolean | — | elevenlabs only: strip background audio, keeping dubbed speech alone. |
| `highest_resolution` | boolean | — | elevenlabs only: render the dubbed video at the highest available resolution. |
| `watermark` | boolean | — | elevenlabs only: watermark the output. |
| `translate_audio_only` | boolean | — | heygen only: translate the audio without altering the speaker's lips. |
| `enable_dynamic_duration` | boolean | — | heygen only: let the output length drift so translated speech fits naturally. Defaults to true. |

### `versely_get_dub`

**Check dub** · read (read-only) · also in the ChatGPT plugin

Get a dubbing project by id — the polling target for versely_create_dub. When the dub has finished, hydrates the inline media card with the dubbed track(s). The dub's card follows progress by itself: only call this when you need the result, a minute or two apart.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `project_id` | string | **yes** | The project id returned by versely_create_dub. |

### `versely_list_dubs`

**List dubs** · read (read-only) · also in the ChatGPT plugin

List the user's dubbing projects, newest first.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `limit` | integer | — | Max projects to return (default 50, max 100). |
| `before` | string | — | Pagination cursor — an ISO timestamp; returns projects created before it. |

### `versely_delete_dub`

**Delete dub** · delete (destructive) · also in the ChatGPT plugin

Delete a dubbing project. If it is still generating it is cancelled and the credits are refunded. Already-rendered output files are kept in storage.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `project_id` | string | **yes** | The project id to delete. |

## Automations

_11 tools_

### `versely_list_slideshow_options`

**List slideshow options** · read (read-only) · also in the ChatGPT plugin

What slideshows and slideshow automations can use: image models, slide shapes, caption styles, subject categories and the user's brands. Read it before creating a slideshow automation.

_No input parameters._

### `versely_estimate_slideshow_automation`

**Estimate automation cost** · read (read-only) · also in the ChatGPT plugin

Credits one run of a slideshow automation would charge, for the given settings, without creating anything. Tell the user this (and how often it runs) before creating or starting one.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source` | enum("ai" \| "pinterest") | — | 'ai' draws new pictures with an image model (default); 'pinterest' uses matching Pinterest pictures. |
| `category` | string | — | Subject area id from versely_list_slideshow_options (e.g. 'fitness'). Needed unless brand_kit_id is set. |
| `brand_kit_id` | string | — | Make every slideshow for one of the user's brands (id from versely_list_slideshow_options). |
| `model` | string | — | Image model for source 'ai', from versely_list_slideshow_options (e.g. 'Flux Pro Ultra'). Required for 'ai'. |
| `num_images` | integer | — | Slides per slideshow, 1-20 (default 5). |
| `content_type` | enum("reel" \| "story" \| "post" \| "portrait" \| "landscape") | — | Shape of the slides (default 'reel', 9:16). |
| `captions` | boolean | — | Plan and burn captions onto the slides (default true). |
| `caption_style` | string | — | Caption style id from versely_list_slideshow_options; omit for classic captions. |
| `post_account_ids` | string[] | — | Post each slideshow to these accounts (ids from versely_list_social_accounts; up to 10). Omit to only save them to the library. |
| `post_caption_prompt` | string | — | Guidance for the auto-written post caption. |

### `versely_create_slideshow_automation`

**Create slideshow automation** · create (open-world) · also in the ChatGPT plugin

Set up a recurring slideshow automation: every run makes a new slideshow (fresh topic each time) for a category or one of the user's brands, and can post it to connected social accounts. Each run spends credits (credits_per_run in the result). It is created paused unless start is true; confirm the settings, the schedule and the cost with the user first. Options: versely_list_slideshow_options.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | string | **yes** | A name for the automation. |
| `every_minutes` | integer | **yes** | How often it runs, in minutes: 5 to 10080 (a week). 60 = hourly, 1440 = daily. |
| `start` | boolean | — | Start now (the first run happens right away). Default false: created paused. |
| `source` | enum("ai" \| "pinterest") | — | 'ai' draws new pictures with an image model (default); 'pinterest' uses matching Pinterest pictures. |
| `category` | string | — | Subject area id from versely_list_slideshow_options (e.g. 'fitness'). Needed unless brand_kit_id is set. |
| `brand_kit_id` | string | — | Make every slideshow for one of the user's brands (id from versely_list_slideshow_options). |
| `model` | string | — | Image model for source 'ai', from versely_list_slideshow_options (e.g. 'Flux Pro Ultra'). Required for 'ai'. |
| `num_images` | integer | — | Slides per slideshow, 1-20 (default 5). |
| `content_type` | enum("reel" \| "story" \| "post" \| "portrait" \| "landscape") | — | Shape of the slides (default 'reel', 9:16). |
| `captions` | boolean | — | Plan and burn captions onto the slides (default true). |
| `caption_style` | string | — | Caption style id from versely_list_slideshow_options; omit for classic captions. |
| `post_account_ids` | string[] | — | Post each slideshow to these accounts (ids from versely_list_social_accounts; up to 10). Omit to only save them to the library. |
| `post_caption_prompt` | string | — | Guidance for the auto-written post caption. |

### `versely_list_automations`

**List automations** · read (read-only) · also in the ChatGPT plugin

List the user's automations with their status (active or paused, and why), schedule, next run, credits per run and spent so far, and the last run's result.

_No input parameters._

### `versely_get_automation`

**Get automation** · read (read-only) · also in the ChatGPT plugin

Get one of the user's automations: its settings, status, schedule and last run.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `automation_id` | string | **yes** | Id from versely_list_automations. |

### `versely_update_automation`

**Edit automation** · edit (destructive, open-world) · also in the ChatGPT plugin

Change an automation's name, schedule or settings. Only the fields given change; the rest are kept. A changed setting applies from the next run.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `automation_id` | string | **yes** | Id from versely_list_automations. |
| `name` | string | — |  |
| `every_minutes` | integer | — | How often it runs, in minutes: 5 to 10080 (a week). 60 = hourly, 1440 = daily. |
| `source` | enum("ai" \| "pinterest") | — | 'ai' draws new pictures with an image model (default); 'pinterest' uses matching Pinterest pictures. |
| `category` | string | — | Subject area id from versely_list_slideshow_options (e.g. 'fitness'). Needed unless brand_kit_id is set. |
| `brand_kit_id` | string | — | Make every slideshow for one of the user's brands (id from versely_list_slideshow_options). |
| `model` | string | — | Image model for source 'ai', from versely_list_slideshow_options (e.g. 'Flux Pro Ultra'). Required for 'ai'. |
| `num_images` | integer | — | Slides per slideshow, 1-20 (default 5). |
| `content_type` | enum("reel" \| "story" \| "post" \| "portrait" \| "landscape") | — | Shape of the slides (default 'reel', 9:16). |
| `captions` | boolean | — | Plan and burn captions onto the slides (default true). |
| `caption_style` | string | — | Caption style id from versely_list_slideshow_options; omit for classic captions. |
| `post_account_ids` | string[] | — | Post each slideshow to these accounts (ids from versely_list_social_accounts; up to 10). Omit to only save them to the library. |
| `post_caption_prompt` | string | — | Guidance for the auto-written post caption. |

### `versely_start_automation`

**Start automation** · edit (open-world) · also in the ChatGPT plugin

Start (or resume) an automation. The next run happens right away, then on its schedule; each run spends credits and posts when it is set to. Also resumes one paused for low credits or repeated failures.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `automation_id` | string | **yes** | Id from versely_list_automations. |

### `versely_pause_automation`

**Pause automation** · edit · also in the ChatGPT plugin

Pause an automation: no more runs until it is started again. A run already in progress finishes.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `automation_id` | string | **yes** | Id from versely_list_automations. |

### `versely_run_automation_now`

**Run automation now** · publish (open-world) · also in the ChatGPT plugin

Make an active automation's next run happen now instead of waiting for its schedule. Spends that run's credits (and posts, when it is set to). Follow it with versely_list_automation_runs.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `automation_id` | string | **yes** | Id from versely_list_automations. |

### `versely_delete_automation`

**Delete automation** · delete (destructive) · also in the ChatGPT plugin

Delete an automation. No more runs; the slideshows it already made stay in the user's library.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `automation_id` | string | **yes** | Id from versely_list_automations. |

### `versely_list_automation_runs`

**List automation runs** · read (read-only) · also in the ChatGPT plugin

An automation's runs, newest first: status, credits charged, a one-line summary, what it made (the slideshow id - open it with versely_get_slideshow) and any error.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `automation_id` | string | **yes** | Id from versely_list_automations. |
| `limit` | integer | — | How many runs (default 20). |

